name: Contributions refresh smoke

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/contributions-smoke.yml'
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
